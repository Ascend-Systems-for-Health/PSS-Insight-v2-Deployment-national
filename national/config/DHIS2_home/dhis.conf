connection.dialect = org.hibernate.dialect.PostgreSQLDialect
connection.driver_class = org.postgresql.Driver
connection.url = jdbc:postgresql://db/dhis2
connection.username = dhis
connection.password = dhis
# Database schema behavior, can be validate, update, create, create-drop
connection.schema = update

encryption.password = xxxx
metadata.audit.persist = on

# External base URL of this instance, so DHIS2 generates redirects/links without dropping the port.
# Served over plain HTTP on port 80 via the nginx gateway (no TLS for this AS-IS demo).
server.base.url = http://137.184.55.221
server.https = off

# URLs for specifying a custom app hub, these are the defaults
#apphub.base.url = https://apps.dhis2.org
#apphub.api.url = https://apps.dhis2.org/api
